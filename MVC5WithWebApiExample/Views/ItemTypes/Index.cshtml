
@{
    ViewBag.Title = "Index";
}


<div class="container mt-3">
    <div class="row offset-3 col-6">
    <h2 class="text-center">Item Types</h2>
        <table id="tblItems" class="table table-striped table-responsive-sm table-striped">
            <thead>
                <tr>
                    <td>Code</td>
                    <td>Name</td>
                </tr>
            </thead>
        </table>
    </div>
</div>

<script>
    $(document).ready(() => {
        $.ajax({
            url: '/ItemTypes',
            method: 'GET',
            success: (response) => {
                $.each(response, (ind,val) => {
                    $('#tblItems').append(`
                        <tr>
                            <td>${val.Code}</td>
                            <td>${val.Name}</td>
                        </tr>
                    `)
                })
            },
            error: (error) => {
                console.log(error);
            }
        })
    })
</script>